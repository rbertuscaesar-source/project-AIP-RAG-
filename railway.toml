[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python startup.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[environments.production.variables]
PYTHON_VERSION = "3.11"
